<%= form_with(model: import_politician, local: true) do |form| %>
  <% if import_politician.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(import_politician.errors.count, "error") %> prohibited this import_politician from being saved:</h2>

      <ul>
      <% import_politician.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
